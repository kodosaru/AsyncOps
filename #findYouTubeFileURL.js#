exports.findYouTubeFileURL = function (inURL){

	// retrieve arguments as array
	var args = [];
	for (var i = 0; i < arguments.length; i++) {
		args.push(arguments[i]);
	}
	console.log("inURL: ",inURL);
	return "dummy";
}
